import { Colors } from './colors';

export enum TColor {
  BLUE = Colors.BLUE,
  RED = Colors.RED,
  BLACK = Colors.BLACK,
  WHITE = Colors.WHITE,
  GREEN = Colors.GREEN,
  GRAY = Colors.GRAY,
}

export enum TSize {
  VERY_SMALL = 12,
  SMALL = 13,
  NORMAL = 14,
  MEDIUM = 16,
  BIG = 18,
}

export const LINE_HEIGHT = 1.4;

import styled from 'styled-components/native';

import { PADDING_SCREEN } from '../../config/constants';

const HorizontalLimitator = styled.View`
  width: auto;
  padding: 0 ${PADDING_SCREEN}px;
`;

export default HorizontalLimitator;
